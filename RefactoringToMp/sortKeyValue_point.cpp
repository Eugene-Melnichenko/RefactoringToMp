#include "include.h"

void sortKeyValue_point(vector<vector<MyStruct>>& type_vector, string keys[3])
{

	for(int ind=0; ind < type_vector.size(); ind++) {
		for(int elm = 0; elm < type_vector[ind].size(); elm++ ){

			if (type_vector[ind][elm].key == keys[0] || type_vector[ind][elm].key == keys[1] || type_vector[ind][elm].key == keys[2])
			{
			
			}

		}
	}

	system("pause");
}